# 28 — Session Handoff

Source of truth for process: `SYSTEM_PROMPT.md` §31, §87 (`System_Prompt/Part2.md`, `Part5.md`). Updated at the end of every session; live draft during a session lives in `session/session-handoff.md`.

## Latest handoff — 2026-07-18 (Phase 9)

**Current phase:** 9 — Mobile. **Complete.**

**Completed work:** Shipped a new Expo/React Native app, `apps/mobile` (`@pee/mobile`), against the exit criteria's "or an equivalent" clause: reuse the same API and design conventions, and host `packages/local-client` or a faithful equivalent, no rewrite. Confirmed by reading the code that literal reuse is technically impossible — Prisma's query engine has no Android/iOS binary target, and Hermes/JSC is not a Node.js runtime — so `adr/0008` documents the resulting decision: a new `MobileStore` (`db/mobile-store.ts` facade over `projects-repo.ts`/`goals-repo.ts`/`tasks-repo.ts`/`outbox-repo.ts`, all backed by `expo-sqlite`, with `LocalStore`'s exact method surface and table shapes) and a `MobileSyncClient` that is a line-for-line port of `SyncClient`'s pull/push/conflict-resolution algorithm — `@pee/types` and the `services/sync` REST contract stay completely unmodified. `auth/mobile-auth-session.ts` mirrors `apps/desktop`'s `AuthSession` (same BFF discipline), substituting `expo-secure-store` for `safeStorage`; since React Native has no main/renderer split, it's exposed via React Context (`auth-context.tsx`) rather than IPC. `api/remote-client.ts` provides the same online-only execution/AI/analytics passthroughs as Desktop. `sync/background-sync.ts` + `sync-context.tsx` provide manual + 30s-interval sync. A NativeWind renderer (`navigation/`, `screens/`, `components/`) reuses `apps/web`'s Tailwind conventions — Login, Projects, GoalDetail (+ AI suggestions panel), Analytics, SyncStatusBadge. Wrote 17 unit tests (256 total across the workspace, all passing) — `MobileStore`/`MobileSyncClient` tested against a *real* embedded SQLite via Node's built-in `node:sqlite` (not a hand-rolled parser), `MobileAuthSession`, and `LoginScreen`. Wrote a Detox e2e spec (`e2e/mobile.e2e.ts`, `.detoxrc.js`) but — unlike Desktop's Electron e2e, which found a real headless-launch path — it could **not** be run in this sandbox (no Android emulator/iOS Simulator available, and no Detox equivalent to Electron's headless trick); honestly documented as unrun rather than claimed as verified. Every new/edited file stayed under the 300-line cap (largest: `screens/GoalDetailScreen.tsx` at 138 lines). Along the way, fixed several real integration issues discovered during setup: a duplicate-hoisted `react-native` copy broke NativeWind's `className` type augmentation (fixed by augmenting directly against this app's own `react-native` import); `expo-crypto`'s Jest auto-mock returned `undefined` for `randomUUID()`, breaking every `MobileStore.create*` call (fixed with an explicit `jest.mock`); `@testing-library/react-native@14`'s `render`/`fireEvent` are now `Promise`-returning APIs requiring `await` throughout. CI gained a "Mobile app unit tests" step; no Detox CI step was added, since it can't run here. Updated `02-prd.md`, `08-backend-guidelines.md`, `12-security.md`, `16-roadmap.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `20-known-issues.md`, `21-decision-log.md`, `27-backlog.md` (extending the local-SQLite-at-rest-encryption and offline-sync-scope entries to cover mobile too; adding Detox-CI-runner/EAS-signing/mobile-payload-validation entries), plus `dashboard/*` and `session/*`.

**Pending work:** Phase 10 (Enterprise) — not yet scoped. Before that, Docker-dependent gaps remain, still unresolved: (1) no integration/e2e suite requiring Postgres has ever been run (auth, projects, planning, execution, sync, local-client, AI, analytics, or frontend — `apps/desktop`'s e2e is the one exception, already run), and (2) no Prisma migration has ever been generated, so nothing has actually been applied to a real Postgres yet. New this phase: (3) `apps/mobile`'s Detox e2e spec has never been run — needs a real Android emulator/iOS Simulator, neither available in this sandbox. Carried forward from Phase 6, unchanged: (4) no automated test calls the live Anthropic/OpenAI APIs. Carried forward from Phase 8, now doubled: (5) both `apps/desktop`'s and `apps/mobile`'s local SQLite files are unencrypted at rest and now genuinely ship on two device classes — revisit before any real end-user distribution.

**Known issues:** See [20-known-issues.md](20-known-issues.md) — `npm audit` findings needing a Nest 11/Next 16 major-version bump plus 24 new findings inside Expo's own build-tooling dependency chain (all tracked, none force-upgraded); no migration ever generated; non-status-change project/goal/task actions still not audit-logged; `apps/web` still has no offline capability itself; `ExecutionEvent`/`AIRecommendation` not covered by sync (both clients' execution/AI/analytics stay online-only for the same reason); local SQLite files unencrypted at rest on both clients; `apps/desktop`'s IPC handlers and `apps/mobile`'s direct `MobileStore` calls both lack `class-validator`-style payload validation; no code signing/auto-update/CI packaging matrix for `apps/desktop`, no EAS cloud build/signing for `apps/mobile`; `apps/mobile`'s Detox e2e never run (no emulator/Simulator in this sandbox); most Docker-dependent e2e specs unexecuted in this sandbox (desktop's is the one exception); no live-vendor smoke test for `services/ai`; analytics computes every metric via live query aggregation with no materialized rollup.

**Open decisions:** None blocking Phase 10. `AIProvider.stream()`/`.embed()`, automatic multi-provider failover, additional AI-native features, AI-recommendation sync coverage, AI usage/cost tracking, materialized analytics rollups, analytics charting upgrade, browser-side offline for `apps/web`, local-file-at-rest encryption on both native clients, extending the sync scope for offline execution/AI/analytics on both clients, desktop code signing/auto-update/CI packaging, mobile EAS cloud build/signing, IPC/direct-call payload validation on both clients, running the Detox e2e on a real device/CI runner, richer multi-device conflict resolution, multi-user project/goal sharing, templates/tags, task dependencies/scheduling, session pause/resume, and cross-service distributed tracing are explicitly deferred (see [27-backlog.md](27-backlog.md)), not silently dropped.

**Recommended next task:** In an environment with Docker and a mobile device/emulator: generate and apply the first Prisma migration, then run the remaining Docker-dependent e2e suites (auth, projects, planning, execution, sync, local-client, AI, analytics, frontend); run `apps/mobile`'s Detox e2e on a real Android emulator/iOS Simulator; separately, a real vendor-credentialed smoke test of `services/ai`; separately, revisit local SQLite file-at-rest encryption on both `apps/desktop` and `apps/mobile` before either reaches real users. Then get user direction on Phase 10 (Enterprise) scope.

**Memory-bank files to load next session:** `00-project-vision.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `29-next-task.md` (standard priority order, `claude/STARTUP.md`).
