# 20 — Known Issues / Risk Register

Source of truth for process: `SYSTEM_PROMPT.md` §82 (`System_Prompt/Part5.md`).

## Open risks

| Risk | Type | Impact | Mitigation |
|---|---|---|---|
| `apps/web` itself has no offline capability | Technical | Local-First (Principle 2) isn't realized in the shipped web product yet, even though the sync protocol now exists and Phases 8-9 proved it out for desktop and mobile | Phase 5 built the protocol (`services/sync`) and a reusable reference client (`packages/local-client`); Phase 8 consumed it for `apps/desktop`, Phase 9 ported it for `apps/mobile`, but `apps/web` itself (100% server-rendered, no client-side storage) remains unretrofitted — see [27-backlog.md](27-backlog.md) |
| `ExecutionEvent` is not covered by the sync protocol | Technical | Neither a future offline web UI, `apps/desktop`, nor `apps/mobile` can show the activity timeline while disconnected | Deliberately descoped from Phase 5's registry (bidirectional-only, 3 entities) rather than forcing an awkward pull-only shape into it; already served online by `GET /goals/:goalId/activity` (and by both clients' online-only execution passthrough). Revisit if/when an offline activity view is actually needed — see [27-backlog.md](27-backlog.md) |
| `packages/local-client`'s SQLite file (and `apps/mobile`'s ported equivalent) is unencrypted at rest, and now genuinely ships on two device classes (Phases 8-9) | Security | A lost/stolen device running `apps/desktop` or `apps/mobile` exposes plain-text `Project`/`Goal`/`Task` data — Phase 5 flagged this as "revisit before Phase 8 ships this file to real end-user machines," and Phases 8-9 now do exactly that on desktop and mobile | No worse than the current Postgres-side posture (disk encryption is an infra concern there too), but this is no longer a future concern on either client. Elevated priority in [27-backlog.md](27-backlog.md) |
| `apps/desktop`'s IPC handlers and `apps/mobile`'s direct `MobileStore` calls don't validate payload shape with `class-validator` the way every HTTP DTO does | Security | A compromised renderer/screen (e.g. a future supply-chain issue in a dependency) could send malformed data straight to `LocalStore`/`MobileStore`, unlike the public HTTP API's `whitelist`/`forbidNonWhitelisted` validation | Both clients run code this project authored (not arbitrary internet input); desktop additionally sandboxes via `contextIsolation`/`sandbox`. Risk is lower than a public endpoint, but not zero. Add `class-validator` DTOs to both boundaries as a follow-up — see [27-backlog.md](27-backlog.md) |
| `apps/desktop` has no code signing, auto-update, or cross-platform CI packaging matrix; `apps/mobile` has no EAS cloud build/signing | Technical/Release | `electron-builder.yml` produces an unsigned local build only; `eas.json` defines dev/preview profiles only, no production profile; neither has a distribution pipeline | Deliberately scoped out of Phases 8-9 — signing certs and packaging matrices are a distribution concern, not a "does the app work" concern. See [27-backlog.md](27-backlog.md) |
| `apps/mobile`'s Detox e2e spec (`e2e/mobile.e2e.ts`) has never been run | Testing | Unlike `apps/desktop`'s Playwright/Electron e2e (which launched headlessly and passed in this sandbox), Detox requires a real Android emulator or iOS Simulator — neither is installable in this sandbox, and there is no headless-launch equivalent | Written and CI-wired (config: `apps/mobile/.detoxrc.js`) for whenever a real device/CI runner is available. Honestly documented as unrun rather than claimed as verified — everything else (17 unit tests, typecheck, lint, build) was actually run and passes |
| `npm audit` flags 24 findings (20 moderate, 4 high) inside Expo's own CLI/prebuild tooling (`@expo/config-plugins`'s `xcode` dependency chain), introduced by adding `apps/mobile` (2026-07-18) | Dependency | These are build-time tooling dependencies (used by `expo prebuild`/`eas build`), not code shipped in the app bundle running on a device | Deliberately deferred, same posture as the pre-existing Next.js/NestJS transitive advisories — `npm audit fix --force` would pull in an untested Expo SDK major mid-feature. Revisit as a dedicated upgrade task |
| SYSTEM_PROMPT.md's example repo layout (§36) and memory-bank file list (§17) are illustrative, not exhaustive | Architectural | Could cause drift if followed too literally once real structure is chosen | Treat as example; record actual structure decisions as ADRs, update [03-system-architecture.md](03-system-architecture.md) |
| `npm audit` (2026-07-17) reports transitive advisories in the NestJS 10.x/Express chain and Next.js 14.x that only clear via a major-version bump (Nest 11, Next 16) | Dependency | Known CVEs (DoS-class, one critical) remain until upgraded | Deliberately deferred rather than force-upgrading mid-feature (`npm audit fix --force` would pull in untested majors); revisit as a dedicated upgrade task, re-run full test suite before merging |
| Auth/Projects/Planning/Execution/Sync/AI/Analytics modules' integration/e2e tests (`services/auth/test/auth.e2e-spec.ts`, `services/projects/test/projects.e2e-spec.ts`, `services/planning/test/planning.e2e-spec.ts`, `services/execution/test/execution.e2e-spec.ts`, `services/sync/test/sync.e2e-spec.ts`, `packages/local-client/test/sync-roundtrip.e2e-spec.ts`, `services/ai/test/ai.e2e-spec.ts`, `services/analytics/test/analytics.e2e-spec.ts`) and the frontend Playwright specs (`apps/web/e2e/login.spec.ts`, `apps/web/e2e/projects.spec.ts`, `apps/web/e2e/planning.spec.ts`, `apps/web/e2e/execution.spec.ts`) require a real Postgres (Docker) and were not executed in the authoring sandbox (no Docker available there) | Testing | Full flows — including the Phase 3 closed-loop progress rollup, the Phase 4 start/complete/activity flow, the Phase 5 sync/conflict-resolution flow, and the Phase 7 owner-isolation guarantee — are untested against a live database/browser until first run | Written to spec and wired into `.github/workflows/ci.yml`, which does have Postgres; run locally via `docker compose -f infrastructure/docker/docker-compose.dev.yml up -d` then `npm run test:e2e -w @pee/auth` / `-w @pee/projects` / `-w @pee/planning` / `-w @pee/execution` / `-w @pee/sync` / `-w @pee/local-client` / `-w @pee/ai` / `-w @pee/analytics` / `-w web` before trusting them fully. **Exception (Phase 8):** `apps/desktop/e2e/desktop.spec.ts` needs no Postgres/Docker (pure SQLite/Electron) and was actually run in the authoring sandbox — it passed, including a real first-run SQLite bootstrap |
| ~~No Prisma migration file has ever been generated~~ | Technical | **Resolved 2026-07-19 (P0 hardening).** `packages/database/prisma/migrations/20260719000000_init/` now exists, generated via `prisma migrate diff --from-empty --to-schema-datamodel` (no live database needed for a from-empty diff). `prisma migrate deploy` in CI now has something real to apply. | Caveat: the migration was generated by schema diff, not `prisma migrate dev` against a live database — it has not yet been applied to a real Postgres instance in this sandbox (still no Docker available). Re-verify `migrate deploy` succeeds and every e2e suite passes against the real resulting schema the first time this runs in an environment with Docker. |
| Project/goal/task **non-status-change** CRUD actions (create, rename, description edit) are not audit-logged | Security/Observability | No record of who created a project or renamed a goal/task, unlike auth events or status transitions | `ExecutionEvent` (Phase 4) now covers status transitions on `Goal`/`Task` unconditionally; a general domain-entity audit trail covering *all* CRUD (including non-status fields, and `Project` entirely) is deferred until a real need (compliance, support debugging) identifies what to capture — see [27-backlog.md](27-backlog.md) |
| No automated test exercises a real network call to the live Anthropic/OpenAI APIs (Phase 6) | Testing | `services/ai`'s actual wire compatibility with either vendor's current API is unverified — `ai.e2e-spec.ts` proves the HTTP/persistence/domain-integration plumbing via an in-test fake-provider override, and unit specs mock the vendor SDKs | Run a manual smoke test with real vendor credentials (`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`) before relying on this feature in a live environment — normal posture for third-party LLM integrations, not a blocker for calling Phase 6 complete |
| `AIRecommendation` (Phase 6) is not covered by the Phase 5 sync protocol | Technical | A future offline UI can't show pending/past AI suggestions while disconnected | Same precedent as `TaskExecutionSession`/`ExecutionEvent` — no offline UI exists yet to need it; the sync registry's bidirectional-only shape would need rework to add a 4th entity. See [27-backlog.md](27-backlog.md) |
| Analytics (Phase 7) computes every metric via live query aggregation, no materialized rollup table | Technical | At real scale (thousands of `TaskExecutionSession`/`ExecutionEvent` rows per user), the bounded date-range queries stay cheap, but this hasn't been load-tested | Deliberately chosen over a summary table or scheduled job — current per-user data volumes are small and adding that infrastructure now would be speculative (Sustainable Complexity, Principle 8). Revisit if real usage data shows query cost becoming an issue — see [27-backlog.md](27-backlog.md) |
| Neither OIDC nor SAML SSO has been exercised against a real or fully browser-mocked IdP in this sandbox (Phase 10) | Testing | The full browser login round-trip (`apps/web` → IdP → back) is unverified end-to-end | The SAML SP's cryptographic assertion-validation path *does* have a genuine, passing unit test (fail-closed against garbage/unsigned input, real `@node-saml/node-saml` library, no mock) and `SsoProvisioningService`'s find-or-create logic is fully unit-tested — only the full browser round-trip against a live IdP is untested. Honestly documented, same class of gap as Detox/AI-vendor — see [27-backlog.md](27-backlog.md) |
| SAML bridge's one-time-code replay guard is an in-memory `Set` (Phase 10) | Technical | In a multi-instance deployment, a code redeemed on instance A wouldn't be marked spent on instance B — a narrow replay window | Documented limitation, acceptable for a single-instance deployment; a shared store (Redis, or a DB table) would be needed before scaling `@pee/auth` horizontally — see [27-backlog.md](27-backlog.md) |
| SAML bridge's `token` endpoint doesn't validate `client_id`/`client_secret` (Phase 10) | Security | Assumes a single self-hosted client (`apps/web`); a multi-tenant-of-third-parties SAML bridge would need real client registration/validation | Acceptable for this product's actual shape (one first-party frontend, not a public OAuth AS) — documented as a known, bounded gap, not silently accepted — see [27-backlog.md](27-backlog.md) |
| Org invite-by-email can reveal account existence to an authenticated org `ADMIN`/`OWNER` (Phase 10) | Security | `inviteMember` returns a distinguishable "no account" error, a narrow email-enumeration surface, lower-risk than a public form since it requires already being an org admin | Documented trade-off; resolved once email-token-based invites (no existing-account requirement) are built — see [27-backlog.md](27-backlog.md) |
| `services/organizations`'s and the extended `services/projects`/`apps/web` e2e specs have never been run against a real Postgres/browser (Phase 10) | Testing | Same carried-forward Docker-dependent gap as every prior phase's integration/e2e suite | Written and CI-wired (`services/organizations/test/organizations.e2e-spec.ts`, extended `services/projects/test/projects.e2e-spec.ts`, `apps/web/e2e/organizations.spec.ts`); dry-compiled in this sandbox (confirmed no syntax/type errors, fails only at the expected "no Postgres" point) but not genuinely executed |

| `services/api`'s CI health-check retry loop (`for i in $(seq 1 30); do curl -sf .../health ...`) previously always succeeded instantly because `/health` was a hardcoded `{status:'ok'}` with no database check — every e2e/Playwright run downstream of it may have been exercising a backend whose actual Postgres connectivity was never verified | Testing | Resolved 2026-07-19 (P0 hardening) — `/health` now runs a real `SELECT 1` and returns 503 if Postgres is unreachable, so the same CI wait-loop is now a genuine readiness gate, not a liveness-only check | No further action; re-verify the CI wait-loop still passes the first time it runs against the now-real migration (see the resolved migration row above) |
| `services/auth`'s SAML bridge (`SamlBridgeService.secret()`) silently fell back to `JWT_ACCESS_SECRET`, then to `''`, if `SSO_SAML_BRIDGE_SECRET` was unset — a real secret-reuse-across-trust-domains bug, and a fallback to an empty signing key if neither was set | Security | Resolved 2026-07-19 (P0 hardening) — the method now throws `ServiceUnavailableException` (503) with no fallback of any kind if `SSO_SAML_BRIDGE_SECRET` is missing; regression test added in `services/auth/test/sso/saml/saml-bridge.service.spec.ts` asserting `JWT_ACCESS_SECRET` is never even read | None — `.env.example` also now documents all 11 `SSO_*` variables actually referenced in code (previously undocumented) |
| P0 hardening findings not covered above (no structured logging/request correlation, no client-side request timeouts, no boot-time env validation, no deployment target) | Operational | Resolved 2026-07-19 — see [17-phase-status.md](17-phase-status.md)'s "Post-Phase-10 — Production Hardening (P0)" section for the full list and what changed | The independent audit's P1 list (N+1 query, unbatched sync push, `npm audit`/Dependabot CI gate, API versioning decision, SAML replay-guard eviction, `/organizations` pagination consistency, README rewrite) is tracked in [27-backlog.md](27-backlog.md), not yet actioned |

Product-code risks begin with Phase 1 (Authentication), implemented 2026-07-17; Phase 2 (Projects), Phase 3 (Planning Engine), Phase 4 (Execution Engine), Phase 5 (Memory Engine), Phase 6 (AI Integration), Phase 7 (Analytics), Phase 8 (Desktop), Phase 9 (Mobile) added 2026-07-18; Phase 10 (Enterprise) added 2026-07-18; P0 production-hardening fixes added 2026-07-19. Add entries here as soon as new issues are discovered; never leave a known risk undocumented.
