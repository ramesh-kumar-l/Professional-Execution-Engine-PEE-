# design-system/ — Design Specification

Written design specification for the Professional Execution Engine UI (`SYSTEM_PROMPT.md` Part 6, §89-§109): typography, spacing, color, icons, layout, navigation, motion, accessibility, dark/light mode, dashboard patterns, forms, tables, command palette, loading/empty/error states, responsive rules, component inventory, interaction principles.

**When to load:** before any frontend implementation work.

**When to update:** when a new reusable UI pattern is introduced (`SYSTEM_PROMPT.md` §108).

Populated in Group 8 (`tokens.md`, `layout-and-navigation.md`, `components.md`, `states.md`, `accessibility-and-themes.md`, `interaction-principles.md`). `project-memory-bank/23-ui-design-system.md` and `24-component-library.md` hold the memory-bank-level summary and pointer here; this folder holds the full specification. Concrete token values/components remain TBD until the first frontend implementation task.
