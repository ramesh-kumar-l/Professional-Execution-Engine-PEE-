# 24 — Component Library

**Status: TBD — no components exist yet.** No frontend implementation has started (Phase 0 = EOS bootstrap only). This file becomes the live inventory of actual reusable components (tokens/primitives/components/patterns per `design-system/` hierarchy) once frontend work begins.

Before adding a new component, check this inventory first — reuse or extend an existing primitive rather than creating a one-off (`SYSTEM_PROMPT.md` §93, `claude/FRONTEND.md`).
