# 28 — Session Handoff

Source of truth for process: `SYSTEM_PROMPT.md` §31, §87 (`System_Prompt/Part2.md`, `Part5.md`). Updated at the end of every session; live draft during a session lives in `session/session-handoff.md`.

## Latest handoff — 2026-07-18 (Phase 6)

**Current phase:** 6 — AI Integration. **Complete.**

**Completed work:** Built `adr/0006`'s deferred `AIProvider` abstraction and shipped its first real feature on the Phase 1-5 foundation. New `services/ai`: `provider/` (`AIProvider` interface exposing only `complete()` — `stream`/`embed` deliberately deferred; `AI_PROVIDER_TOKEN`-keyed DI factory constructing only the vendor named by `AI_PROVIDER`, failing fast at boot if that vendor's key is missing; `AnthropicProvider` — forced tool-use for structured output; `OpenAIProvider` — `response_format: json_schema`; both wrapped in an explicit ~20s `AbortController` timeout and mapped to a typed `AIProviderError`) and `recommendations/` (`AIRecommendationsService` — `generateTaskSuggestions` builds a bounded prompt (goal + up to 50 existing task titles), persists an `AIRecommendation` as `PENDING` with full reason/confidence/alternatives/context, never auto-creating a `Task`; `accept` reuses `TasksService.create` per selected suggestion so the Phase 3 rollup fires identically to any other task creation; `dismiss` creates nothing; a provider failure or malformed structured output is caught and persisted as `FAILED`, never fabricated). New `AIRecommendation` table (`packages/database`). New AI types (`packages/types`). `services/api` wired with `AIModule`. `apps/web`'s goal detail page gained an "AI Suggestions" panel (suggest/accept-selected/dismiss, all plain Server Actions — no new architectural pattern). A shared `ai-provider.contract.spec.ts` proves both vendor implementations satisfy the same behavioral contract (request/response shape, real timeout/abort wiring) — this is what "multi-provider safe" is actually verified by. `ai.e2e-spec.ts` overrides `AI_PROVIDER_TOKEN` with an in-test fake provider, so it needs Docker Postgres but **no vendor API keys** — a genuine testability improvement over every prior phase's e2e posture. Added an AI e2e CI step + `AI_PROVIDER`/`ANTHROPIC_API_KEY`/`ANTHROPIC_MODEL` CI env vars (placeholder key, since the real app now fail-fasts at boot without one). Wrote 35 backend unit/DTO tests (183 total across the workspace, all passing). Every new/edited file stayed well under the 300-line cap (largest new file: `ai-recommendations.service.ts` at 212 lines). Verified against `evaluation/ai-feature-quality-bar.md`'s 8 criteria and `checklists/security-checklist.md`. Updated `02-prd.md`, `08-backend-guidelines.md`, `09-ai-architecture.md`, `10-database-design.md`, `11-api-contract.md`, `12-security.md`, `16-roadmap.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `20-known-issues.md`, `21-decision-log.md`, `27-backlog.md`, plus `dashboard/*` and `session/*`.

**Pending work:** Phase 7 (Analytics) — not yet scoped. Before that, Docker-dependent gaps remain, still unresolved: (1) no integration/e2e suite has ever been run (auth, projects, planning, execution, sync, local-client, AI, or frontend), and (2) no Prisma migration has ever been generated, so nothing has actually been applied to a real Postgres yet. New in Phase 6: (3) no automated test calls the live Anthropic/OpenAI APIs — a real vendor-credentialed smoke test is recommended before relying on this feature in production.

**Known issues:** See [20-known-issues.md](20-known-issues.md) — `npm audit` findings needing a Nest 11/Next 16 major-version bump (tracked, not force-upgraded); no migration ever generated; non-status-change project/goal/task actions still not audit-logged; `apps/web` still has no offline capability itself (protocol exists, web app doesn't consume it — Phase 8/9); `ExecutionEvent`/`AIRecommendation` not covered by sync; local SQLite file unencrypted at rest; all Docker-dependent e2e specs unexecuted in this sandbox; no live-vendor smoke test for `services/ai`.

**Open decisions:** None blocking Phase 7. `AIProvider.stream()`/`.embed()`, automatic multi-provider failover, additional AI-native features, AI-recommendation sync coverage, AI usage/cost tracking, browser-side offline for `apps/web`, local-file-at-rest encryption, richer multi-device conflict resolution, multi-user project/goal sharing, templates/tags, task dependencies/scheduling, session pause/resume, and cross-service distributed tracing are explicitly deferred (see [27-backlog.md](27-backlog.md)), not silently dropped.

**Recommended next task:** In an environment with Docker: generate and apply the first Prisma migration, then run the Docker-dependent e2e suites (auth, projects, planning, execution, sync, local-client, AI, frontend); separately, a real vendor-credentialed smoke test of `services/ai`. Then get user direction on Phase 7 (Analytics) scope.

**Memory-bank files to load next session:** `00-project-vision.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `29-next-task.md` (standard priority order, `claude/STARTUP.md`).
