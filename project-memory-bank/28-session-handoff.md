# 28 — Session Handoff

Source of truth for process: `SYSTEM_PROMPT.md` §31, §87 (`System_Prompt/Part2.md`, `Part5.md`). Updated at the end of every session; live draft during a session lives in `session/session-handoff.md`.

## Latest handoff — 2026-07-18 (Phase 5)

**Current phase:** 5 — Memory Engine. **Complete.**

**Completed work:** Designed and implemented `adr/0003`'s deferred sync protocol — the first real SQLite↔Postgres reconciliation, not a stub — on top of the Phase 1-4 foundation. `Project`/`Goal`/`Task` gained a composite `[ownerId, updatedAt]` index; `version` (declared on every syncable table since Phase 1 per `adr/0003`, but never incremented by any service) is now a live optimistic-concurrency guard on every `update()`/`archive()`. New `services/sync` (`SyncEntityRegistryService` — registry pairing each of the 3 bidirectional entities with its Prisma delegate + domain service; `pull/sync-pull.service.ts` — paginated, cursor-based; `push/sync-push.service.ts` — atomic version-guarded update via `updateMany`, falling back to last-write-wins-by-timestamp on a mismatch; `SyncController` — `POST /sync/pull`, `POST /sync/push`). Sync push deliberately reuses the existing domain services for writes (not raw Prisma) so a synced task completion still triggers the Phase 3 rollup and Phase 4 `ExecutionEvent` — sync never bypasses those guarantees. New `packages/local-client` (not a Nest module — a plain library with its own SQLite Prisma schema/client, `LocalStore` for offline-capable local CRUD + an outbox, `SyncClient` for `pull`/`push` over `fetch`) — the reusable reference client proving the protocol against a real embedded database, intended for Phase 8/9 (Desktop/Mobile) to build on rather than re-invent. `apps/web` was deliberately left untouched (confirmed 100% server-rendered, no client-side storage — retrofitting it now would be a disproportionate rewrite; that's Phase 8/9's job). Added sync + local-client e2e steps to `.github/workflows/ci.yml`. Wrote 28 backend unit/DTO tests (`@pee/sync`) + 13 tests against a real (temp-file) SQLite database (`@pee/local-client`, no Docker needed) + extended the Projects/Goals/Tasks specs with id/updatedAt-passthrough and version-increment assertions (148 tests total across the workspace, all passing) plus two integration/e2e specs requiring Docker Postgres. Every new/edited file stayed well under the 300-line cap (largest new file: `local-store.ts` at 159 lines). Updated `02-prd.md`, `08-backend-guidelines.md`, `10-database-design.md`, `11-api-contract.md`, `12-security.md`, `16-roadmap.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `20-known-issues.md`, `21-decision-log.md`, `27-backlog.md`, plus `dashboard/*` and `session/*`.

**Pending work:** Phase 6 (AI Integration) — not yet scoped. Before that, two Docker-dependent gaps remain, still unresolved: (1) no integration/e2e suite has ever been run (auth, projects, planning, execution, sync, local-client, or frontend), and (2) no Prisma migration has ever been generated, so nothing has actually been applied to a real Postgres yet.

**Known issues:** See [20-known-issues.md](20-known-issues.md) — `npm audit` findings needing a Nest 11/Next 16 major-version bump (tracked, not force-upgraded); no migration ever generated; non-status-change project/goal/task actions still not audit-logged; `apps/web` still has no offline capability itself (protocol exists, web app doesn't consume it — Phase 8/9); `ExecutionEvent` not covered by sync; local SQLite file unencrypted at rest; all Docker-dependent e2e specs unexecuted in this sandbox.

**Open decisions:** None blocking Phase 6. Browser-side offline for `apps/web`, `ExecutionEvent`/`TaskExecutionSession` sync coverage, local-file-at-rest encryption, richer multi-device conflict resolution, multi-user project/goal sharing, templates/tags, task dependencies/scheduling, AI-assisted plan generation, session pause/resume, and cross-service distributed tracing are explicitly deferred (see [27-backlog.md](27-backlog.md)), not silently dropped.

**Recommended next task:** In an environment with Docker: generate and apply the first Prisma migration, then run the Docker-dependent e2e suites (auth, projects, planning, execution, sync, local-client, frontend). Then get user direction on Phase 6 (AI Integration) scope.

**Memory-bank files to load next session:** `00-project-vision.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `29-next-task.md` (standard priority order, `claude/STARTUP.md`).
