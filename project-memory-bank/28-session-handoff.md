# 28 — Session Handoff

Source of truth for process: `SYSTEM_PROMPT.md` §31, §87 (`System_Prompt/Part2.md`, `Part5.md`). Updated at the end of every session; live draft during a session lives in `session/session-handoff.md`.

## Latest handoff — 2026-07-18 (Phase 8)

**Current phase:** 8 — Desktop. **Complete.**

**Completed work:** Shipped a new Electron app, `apps/desktop` (`@pee/desktop`), against the literal exit criteria: reuse the same API, design conventions, and `packages/local-client`, no rewrite. `adr/0007` documents choosing Electron over Tauri specifically because Electron's Node.js main process can import `@pee/local-client`'s `LocalStore`/`SyncClient` completely unmodified — Tauri's Rust backend would have forced a reimplementation. Main process: `local-store-factory.ts` (first-run SQLite bootstrap via `prisma db push`, mirroring `@pee/local-client`'s own test fixture), `auth/auth-session.ts`+`auth-ipc.ts` (login/refresh/logout against `services/auth`, `safeStorage`-encrypted refresh-token custody — the same BFF discipline as `apps/web`, relocated), `ipc/projects-ipc.ts`/`goals-ipc.ts`/`tasks-ipc.ts` (offline CRUD via `LocalStore`), `ipc/sync-ipc.ts` (manual + 30s-interval background sync via the unmodified `SyncClient`, with refresh-on-failure retry), `ipc/remote-ipc.ts` (online-only passthroughs to `services/execution`/`services/ai`/`services/analytics` — deliberately not added to `@pee/local-client`'s sync registry, which stays Project/Goal/Task only). Preload (`electron/preload/index.ts`) exposes a narrow typed `contextBridge` surface; `contextIsolation`/`sandbox` enabled, no raw `ipcRenderer` reachable from the renderer. New React+Vite renderer (`src/`) reuses `apps/web`'s Tailwind conventions (no shared design-system package exists to reuse instead) — Login, Projects, GoalDetail (+ AI suggestions panel), Analytics, SyncStatusBadge. Wrote 31 unit tests (239 total across the workspace, all passing) plus a Playwright Electron e2e spec that — unlike every prior phase's e2e — needs no Docker/Postgres at all, and was **actually run and passed** in this sandbox: it launched the real built app and confirmed the login screen renders, having genuinely bootstrapped a local SQLite file with all 6 expected tables via `prisma db push`. Also fixed a real bug found during that manual verification: an unpackaged dev/test run without `app.setName()` resolves `userData` to Electron's generic shared folder, colliding with every other unpackaged Electron app on the machine. Every new/edited file stayed under the 300-line cap (largest: `auth-session.ts` at 139 lines). CI gained an "Electron launch smoke test" step (`xvfb-run npm run test:e2e -w @pee/desktop`) after the Build step. Updated `02-prd.md`, `08-backend-guidelines.md`, `12-security.md`, `16-roadmap.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `20-known-issues.md`, `21-decision-log.md`, `27-backlog.md` (elevating the local-SQLite-at-rest-encryption entry's priority now that it's live, not hypothetical; adding desktop-packaging/IPC-validation/offline-scope-extension entries), plus `dashboard/*` and `session/*`.

**Pending work:** Phase 9 (Mobile) — not yet scoped. Before that, Docker-dependent gaps remain, still unresolved: (1) no integration/e2e suite requiring Postgres has ever been run (auth, projects, planning, execution, sync, local-client, AI, analytics, or frontend — `apps/desktop`'s e2e is the one exception, already run), and (2) no Prisma migration has ever been generated, so nothing has actually been applied to a real Postgres yet. Carried forward from Phase 6, unchanged: (3) no automated test calls the live Anthropic/OpenAI APIs — a real vendor-credentialed smoke test is recommended before relying on that feature in production. New this phase: (4) `apps/desktop`'s local SQLite file is unencrypted at rest and now genuinely ships — revisit before any real end-user distribution.

**Known issues:** See [20-known-issues.md](20-known-issues.md) — `npm audit` findings needing a Nest 11/Next 16 major-version bump (tracked, not force-upgraded); no migration ever generated; non-status-change project/goal/task actions still not audit-logged; `apps/web` still has no offline capability itself; `ExecutionEvent`/`AIRecommendation` not covered by sync (desktop's execution/AI/analytics stay online-only for the same reason); local SQLite file unencrypted at rest (now live via `apps/desktop`); `apps/desktop`'s IPC handlers lack `class-validator`-style payload validation; no code signing/auto-update/CI packaging matrix for `apps/desktop`; most Docker-dependent e2e specs unexecuted in this sandbox (desktop's is the exception); no live-vendor smoke test for `services/ai`; analytics computes every metric via live query aggregation with no materialized rollup.

**Open decisions:** None blocking Phase 9. `AIProvider.stream()`/`.embed()`, automatic multi-provider failover, additional AI-native features, AI-recommendation sync coverage, AI usage/cost tracking, materialized analytics rollups, analytics charting upgrade, browser-side offline for `apps/web`, local-file-at-rest encryption, extending `@pee/local-client`'s sync registry for offline execution/AI/analytics, desktop code signing/auto-update/CI packaging, IPC payload validation, richer multi-device conflict resolution, multi-user project/goal sharing, templates/tags, task dependencies/scheduling, session pause/resume, and cross-service distributed tracing are explicitly deferred (see [27-backlog.md](27-backlog.md)), not silently dropped.

**Recommended next task:** In an environment with Docker: generate and apply the first Prisma migration, then run the remaining Docker-dependent e2e suites (auth, projects, planning, execution, sync, local-client, AI, analytics, frontend); separately, a real vendor-credentialed smoke test of `services/ai`; separately, revisit local SQLite file-at-rest encryption before `apps/desktop` reaches real users. Then get user direction on Phase 9 (Mobile) scope.

**Memory-bank files to load next session:** `00-project-vision.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `29-next-task.md` (standard priority order, `claude/STARTUP.md`).
