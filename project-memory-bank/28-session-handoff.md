# 28 — Session Handoff

Source of truth for process: `SYSTEM_PROMPT.md` §31, §87 (`System_Prompt/Part2.md`, `Part5.md`). Updated at the end of every session; live draft during a session lives in `session/session-handoff.md`.

## Latest handoff — 2026-07-18 (Phase 3)

**Current phase:** 3 — Planning Engine. **Complete.**

**Completed work:** Implemented the second domain-entity feature on top of the Phase 1 (auth) + Phase 2 (projects) foundation. `Goal`/`Task` models added to `packages/database` (Prisma: `GoalStatus`/`TaskStatus` enums, `Goal` nested under `Project`, `Task` nested under `Goal`, both single-owner via a denormalized `ownerId`); goal/task DTOs added to `packages/types`; `services/planning` (`goals/` + `tasks/` subfolders — `GoalsService`/`TasksService`, four controllers for the nested + flat routes, all `JwtAuthGuard`-protected, ownership enforced with 404-not-403). The core deliverable is the **closed execution loop**: `TasksService` calls `GoalsService.recalculateProgress(goalId)` after every task mutation, so a goal's `status` (`NOT_STARTED`→`IN_PROGRESS`→`COMPLETED`) and computed `progress` update automatically as tasks are completed — no manual step, no separate "recompute" endpoint. Goal creation reuses the already-public `ProjectsService.getOne` to verify project ownership rather than widening `ProjectsService`'s private internals. `services/api` wired to import `PlanningModule`; `apps/web` gained `/dashboard/projects/[id]/goals` (list/new), `/dashboard/goals/[id]` (detail — progress, inline task creation, mark-done/archive), `/dashboard/goals/[id]/edit`, with a new `lib/planning-api-client.ts` (split out from `lib/api-client.ts` to keep both files small) and `GoalForm`/`TaskForm` components (same tested `onSubmit`+`useState` pattern as `ProjectForm`). Added a planning e2e step to `.github/workflows/ci.yml`. Wrote 31 backend unit/DTO tests + 2 new frontend Vitest specs (86 tests total across the workspace, all passing) plus integration/e2e specs requiring Docker Postgres. Every new file stayed well under the 300-line cap (largest: `goals.service.ts` at 151 lines). Updated `02-prd.md`, `08-backend-guidelines.md`, `10-database-design.md`, `11-api-contract.md`, `12-security.md`, `16-roadmap.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `20-known-issues.md`, `21-decision-log.md`, `27-backlog.md`, plus `dashboard/*` and `session/*`.

**Pending work:** Phase 4 (Execution Engine) — not yet scoped. Before that, two Docker-dependent gaps remain from Phase 1, still unresolved: (1) no integration/e2e suite has ever been run (auth, projects, planning, or frontend), and (2) no Prisma migration has ever been generated, so nothing has actually been applied to a real database yet.

**Known issues:** See [20-known-issues.md](20-known-issues.md) — sync protocol still deferred (`adr/0003`); `npm audit` findings needing a Nest 11/Next 16 major-version bump (tracked, not force-upgraded); no migration ever generated; project/goal/task actions not audit-logged; all Docker-dependent e2e specs unexecuted in this sandbox.

**Open decisions:** None blocking Phase 4. Multi-user project/goal sharing, templates/tags, task dependencies/scheduling, AI-assisted plan generation, and a domain-entity audit trail are explicitly deferred (see [27-backlog.md](27-backlog.md)), not silently dropped.

**Recommended next task:** In an environment with Docker: generate and apply the first Prisma migration, then run the Docker-dependent e2e suites (auth, projects, planning, frontend). Then get user direction on Phase 4 (Execution Engine) scope.

**Memory-bank files to load next session:** `00-project-vision.md`, `17-phase-status.md`, `18-current-state.md`, `19-active-work.md`, `29-next-task.md` (standard priority order, `claude/STARTUP.md`).
