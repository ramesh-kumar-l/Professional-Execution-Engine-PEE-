# Metrics

**Status: not yet instrumented.** No product code exists yet, so no runtime metrics (performance, reliability, usage) are available. This file becomes the human-facing summary of the raw observability data defined in `project-memory-bank/14-observability.md` and `docs/standards/observability-and-logging.md`, once a service exists to emit them.

## Targets to track against, once available

Frontend: initial load < 2s, interaction latency < 100ms, navigation latency < 150ms (`project-memory-bank/25-performance-goals.md`). Backend: TBD, set once the stack is chosen.
